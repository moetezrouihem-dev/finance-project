<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Finance Manager | Moetez Rouihem - ENSI</title>
    
    <!-- Bibliothèques Externes -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;700&display=swap" rel="stylesheet">

    <style>
        :root {
            /* Palette de couleurs "ENSI Tech" */
            --bg-dark: #09090b;
            --primary: #3b82f6; /* Bleu Ingénieur */
            --accent: #8b5cf6; /* Violet Tech */
            --success: #10b981;
            --card-bg: rgba(24, 24, 27, 0.6);
            --border: 1px solid rgba(255, 255, 255, 0.08);
            --text-main: #e4e4e7;
            --text-muted: #a1a1aa;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Outfit', sans-serif; }

        body {
            background-color: var(--bg-dark);
            /* Fond animé subtil */
            background-image: 
                radial-gradient(at 0% 0%, rgba(59, 130, 246, 0.15) 0px, transparent 50%),
                radial-gradient(at 100% 100%, rgba(139, 92, 246, 0.15) 0px, transparent 50%);
            color: var(--text-main);
            height: 100vh;
            display: flex;
            overflow: hidden;
        }

        /* --- SIDEBAR (Le Menu) --- */
        aside {
            width: 280px;
            background: rgba(0, 0, 0, 0.4);
            backdrop-filter: blur(20px);
            border-right: var(--border);
            padding: 2rem 1.5rem;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .profile-card {
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(139, 92, 246, 0.1));
            border: var(--border);
            padding: 1.5rem;
            border-radius: 16px;
            text-align: center;
            margin-bottom: 2rem;
        }

        .avatar-circle {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, var(--primary), var(--accent));
            border-radius: 50%;
            margin: 0 auto 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            font-weight: bold;
            color: white;
            box-shadow: 0 0 20px rgba(59, 130, 246, 0.4);
        }

        .student-name { font-size: 1.2rem; font-weight: 700; color: white; }
        .student-info { font-size: 0.85rem; color: var(--text-muted); margin-top: 4px; }
        .school-badge { 
            display: inline-block; 
            margin-top: 8px; 
            padding: 4px 12px; 
            background: rgba(59, 130, 246, 0.2); 
            color: #60a5fa; 
            border-radius: 20px; 
            font-size: 0.75rem; 
            font-weight: 600; 
        }

        nav ul { list-style: none; }
        nav li {
            margin-bottom: 0.5rem;
            padding: 14px;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
            color: var(--text-muted);
            display: flex;
            align-items: center;
            gap: 12px;
            font-weight: 500;
        }
        nav li:hover { background: rgba(255, 255, 255, 0.05); color: white; }
        nav li.active {
            background: var(--primary);
            color: white;
            box-shadow: 0 8px 20px -6px rgba(59, 130, 246, 0.5);
        }

        /* --- CONTENU PRINCIPAL --- */
        main {
            flex: 1;
            padding: 2.5rem;
            overflow-y: auto;
        }

        .page-header {
            margin-bottom: 2rem;
            animation: slideDown 0.6s ease;
        }
        .page-title { font-size: 2rem; font-weight: 700; margin-bottom: 0.5rem; }
        .page-subtitle { color: var(--text-muted); }

        .content-section { display: none; animation: fadeIn 0.4s ease; }
        .content-section.active { display: block; }

        .grid-layout {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 1.5rem;
        }

        .glass-card {
            background: var(--card-bg);
            border: var(--border);
            border-radius: 20px;
            padding: 2rem;
            position: relative;
            overflow: hidden;
        }
        
        .glass-card::before {
            content: '';
            position: absolute;
            top: 0; left: 0; width: 100%; height: 4px;
            background: linear-gradient(90deg, var(--primary), var(--accent));
        }

        /* --- FORMULAIRES --- */
        .form-group { margin-bottom: 1.2rem; }
        label { display: block; margin-bottom: 0.5rem; font-size: 0.9rem; color: var(--text-muted); }
        
        input, select {
            width: 100%;
            padding: 14px;
            background: rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255,255,255,0.1);
            border-radius: 10px;
            color: white;
            font-size: 1rem;
            transition: 0.3s;
        }
        input:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.2);
            outline: none;
        }

        button.action-btn {
            width: 100%;
            padding: 16px;
            margin-top: 10px;
            background: linear-gradient(90deg, var(--primary), var(--accent));
            border: none;
            border-radius: 10px;
            color: white;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            transition: 0.3s;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        button.action-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px -5px rgba(59, 130, 246, 0.4);
        }

        /* --- RESULTATS & TABLES --- */
        .result-display {
            margin-top: 1.5rem;
            padding: 1.5rem;
            background: rgba(16, 185, 129, 0.1);
            border: 1px solid rgba(16, 185, 129, 0.2);
            border-radius: 12px;
            display: none;
        }

        .value-highlight {
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--success);
        }

        table { width: 100%; border-collapse: collapse; margin-top: 1rem; font-size: 0.9rem; }
        th { text-align: left; color: var(--text-muted); padding: 12px; border-bottom: 1px solid rgba(255,255,255,0.1); }
        td { padding: 12px; border-bottom: 1px solid rgba(255,255,255,0.05); }
        tr:last-child td { border: none; }

        /* Animations */
        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
        @keyframes slideDown { from { opacity: 0; transform: translateY(-20px); } to { opacity: 1; transform: translateY(0); } }

    </style>
</head>
<body>

    <!-- BARRE LATERALE -->
    <aside>
        <div>
            <!-- PROFIL ÉTUDIANT -->
            <div class="profile-card">
                <div class="avatar-circle">MR</div>
                <div class="student-name">Moetez Rouihem</div>
                <div class="student-info">Génie Logiciel</div>
                <div class="school-badge">ENSI - 1ère Année</div>
            </div>

            <!-- NAVIGATION -->
            <nav>
                <ul>
                    <li class="active" onclick="switchTab('dashboard', this)">
                        <i class="fas fa-grid-2"></i> Tableau de bord
                    </li>
                    <li onclick="switchTab('epargne', this)">
                        <i class="fas fa-piggy-bank"></i> Intérêts Simples
                    </li>
                    <li onclick="switchTab('escompte', this)">
                        <i class="fas fa-file-invoice-dollar"></i> Escompte
                    </li>
                    <li onclick="switchTab('emprunt', this)">
                        <i class="fas fa-building-columns"></i> Annuités & Emprunts
                    </li>
                    <li onclick="switchTab('portefeuille', this)">
                        <i class="fas fa-chart-pie"></i> Gestion Portefeuille
                    </li>
                </ul>
            </nav>
        </div>
        
        <div style="font-size: 0.75rem; color: #52525b; text-align: center;">
            &copy; 2025 Projet Mathématiques Financières
        </div>
    </aside>

    <!-- ZONE PRINCIPALE -->
    <main>

        <!-- DASHBOARD -->
        <section id="dashboard" class="content-section active">
            <div class="page-header">
                <h1 class="page-title">Bonjour, Moetez 👋</h1>
                <p class="page-subtitle">Prêt pour tes calculs financiers aujourd'hui ?</p>
            </div>
            
            <div class="grid-layout">
                <div class="glass-card">
                    <h3><i class="fas fa-graduation-cap" style="color:var(--primary)"></i> Module Projet</h3>
                    <p style="margin-top:10px; color:var(--text-muted)">
                        Cette application regroupe tous les sujets demandés : 
                        intérêts, escompte, annuités et optimisation.
                    </p>
                </div>
                <div class="glass-card">
                    <h3><i class="fas fa-code" style="color:var(--accent)"></i> Technologie</h3>
                    <p style="margin-top:10px; color:var(--text-muted)">
                        Développé en HTML5, CSS3 Moderne et JavaScript Vanilla. 
                        Aucune dépendance lourde requise.
                    </p>
                </div>
            </div>
        </section>

        <!-- 1. INTÉRÊTS SIMPLES -->
        <section id="epargne" class="content-section">
            <div class="page-header">
                <h1 class="page-title">Intérêts Simples</h1>
                <p class="page-subtitle">Application pour Compte d'Épargne & Courant</p>
            </div>

            <div class="grid-layout">
                <div class="glass-card">
                    <div class="form-group">
                        <label>Capital Initial (C)</label>
                        <input type="number" id="ep_C" placeholder="ex: 5000">
                    </div>
                    <div class="form-group">
                        <label>Taux Annuel % (t)</label>
                        <input type="number" id="ep_t" placeholder="ex: 4">
                    </div>
                    <div class="form-group">
                        <label>Durée de placement (mois)</label>
                        <input type="number" id="ep_n" placeholder="ex: 18">
                    </div>
                    <button class="action-btn" onclick="calculerEpargne()">Calculer le rendement</button>

                    <div id="res_epargne" class="result-display">
                        <div style="font-size:0.9rem; text-transform:uppercase; letter-spacing:1px; color:#10b981;">Valeur Acquise</div>
                        <div id="val_acquise" class="value-highlight">0.00 DT</div>
                        <div style="margin-top:5px; color:var(--text-muted)">Dont intérêts : <span id="val_interets" style="color:white">0.00 DT</span></div>
                    </div>
                </div>
                <div class="glass-card">
                    anvas id="chartEpargne"></canvas>
                </div>
            </div>
        </section>

        <!-- 2. ESCOMPTE -->
        <section id="escompte" class="content-section">
            <div class="page-header">
                <h1 class="page-title">Bordereau d'Escompte</h1>
                <p class="page-subtitle">Calcul de la valeur nette et de l'agio</p>
            </div>

            <div class="grid-layout">
                <div class="glass-card">
                    <div class="form-group">
                        <label>Valeur Nominale de l'effet (V)</label>
                        <input type="number" id="esc_V" placeholder="ex: 12000">
                    </div>
                    <div class="form-group">
                        <label>Jours à courir (n)</label>
                        <input type="number" id="esc_n" placeholder="ex: 60">
                    </div>
                    <div class="form-group">
                        <label>Taux d'escompte % (t)</label>
                        <input type="number" id="esc_t" placeholder="ex: 9">
                    </div>
                    <button class="action-btn" onclick="calculerEscompte()">Générer Bordereau</button>
                </div>

                <div class="glass-card">
                    <h3>Résultat de l'opération</h3>
                    <div id="res_escompte_details" style="margin-top:20px; display:none;">
                        <div style="display:flex; justify-content:space-between; margin-bottom:10px;">
                            <span>Escompte Commercial :</span>
                            <span style="color:#f87171" id="val_esc_comm">- 0.00</span>
                        </div>
                        <hr style="border-color:rgba(255,255,255,0.1); margin:10px 0;">
                        <div style="text-align:center;">
                            <span style="font-size:0.9rem; color:var(--text-muted)">NET À VOTRE CRÉDIT</span>
                            <div class="value-highlight" id="val_esc_net">0.00 DT</div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- 3. ANNUITÉS / EMPRUNTS -->
        <section id="emprunt" class="content-section">
            <div class="page-header">
                <h1 class="page-title">Emprunts Indivis</h1>
                <p class="page-subtitle">Tableau d'amortissement (Annuité Constante)</p>
            </div>

            <div class="glass-card" style="margin-bottom: 20px;">
                <div style="display:grid; grid-template-columns: 1fr 1fr 1fr; gap:20px;">
                    <div class="form-group">
                        <label>Montant Emprunt (K)</label>
                        <input type="number" id="emp_K" value="20000">
                    </div>
                    <div class="form-group">
                        <label>Taux Intérêt % (i)</label>
                        <input type="number" id="emp_i" value="5.5">
                    </div>
                    <div class="form-group">
                        <label>Durée (Années)</label>
                        <input type="number" id="emp_n" value="5">
                    </div>
                </div>
                <button class="action-btn" onclick="calculerEmprunt()">Générer l'échéancier</button>
            </div>

            <div class="glass-card" style="overflow-x: auto;">
                <table id="table_emprunt">
                    <thead>
                        <tr>
                            <th>Année</th>
                            <th>Capital Restant Dû</th>
                            <th>Intérêts</th>
                            <th>Amortissement</th>
                            <th style="color:var(--primary)">Annuité</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr><td colspan="5" style="text-align:center; padding:20px;">Entrez les données et cliquez sur calculer</td></tr>
                    </tbody>
                </table>
            </div>
        </section>

        <!-- 4. GESTION PORTEFEUILLE -->
        <section id="portefeuille" class="content-section">
            <div class="page-header">
                <h1 class="page-title">Optimisation Portefeuille</h1>
                <p class="page-subtitle">Simulation simple à 2 Actifs</p>
            </div>

            <div class="grid-layout">
                <div class="glass-card">
                    <div class="form-group">
                        <label>Rendement Actif A (%)</label>
                        <input type="number" id="pf_r1" value="12">
                    </div>
                    <div class="form-group">
                        <label>Rendement Actif B (%)</label>
                        <input type="number" id="pf_r2" value="4">
                    </div>
                    <div class="form-group">
                        <label>Répartition (Combien sur A ?)</label>
                        <input type="range" id="pf_w1" min="0" max="100" value="50" style="width:100%; margin-top:10px;" oninput="updatePfDisplay(this.value)">
                        <div style="display:flex; justify-content:space-between; margin-top:5px; font-weight:bold;">
                            <span style="color:var(--primary)">A: <span id="lbl_w1">50</span>%</span>
                            <span style="color:var(--accent)">B: <span id="lbl_w2">50</span>%</span>
                        </div>
                    </div>
                    <button class="action-btn" onclick="calculerPortefeuille()">Simuler Rendement</button>
                    
                    <div id="res_pf" class="result-display">
                        <div style="text-align:center;">
                            <span>Rendement Espéré Global</span>
                            <div class="value-highlight" id="pf_global">0.00 %</div>
                        </div>
                    </div>
                </div>
                <div class="glass-card">
                    anvas id="chartPf"></canvas>
                </div>
            </div>
        </section>

    </main>

    <!-- LOGIQUE JAVASCRIPT -->
    <script>
        // --- Navigation ---
        function switchTab(tabId, element) {
            // Cacher toutes les sections
            document.querySelectorAll('.content-section').forEach(sec => sec.classList.remove('active'));
            document.getElementById(tabId).classList.add('active');
            
            // Mettre à jour le menu
            document.querySelectorAll('nav li').forEach(li => li.classList.remove('active'));
            element.classList.add('active');
        }

        // --- Instances Graphiques ---
        let chartEpargneInstance = null;
        let chartPfInstance = null;

        // --- 1. Calcul Epargne ---
        function calculerEpargne() {
            const C = parseFloat(document.getElementById('ep_C').value);
            const t = parseFloat(document.getElementById('ep_t').value);
            const n = parseFloat(document.getElementById('ep_n').value);

            if (!C || !t || !n) return alert("Veuillez remplir tous les champs !");

            // I = C * t/100 * n/12
            const interets = C * (t/100) * (n/12);
            const total = C + interets;

            // Affichage Chiffres
            document.getElementById('res_epargne').style.display = "block";
            document.getElementById('val_acquise').innerText = total.toFixed(2) + " DT";
            document.getElementById('val_interets').innerText = interets.toFixed(2) + " DT";

            // Affichage Graphique
            const ctx = document.getElementById('chartEpargne').getContext('2d');
            if (chartEpargneInstance) chartEpargneInstance.destroy();

            chartEpargneInstance = new Chart(ctx, {
                type: 'doughnut',
                data: {
                    labels: ['Capital Initial', 'Intérêts Gagnés'],
                    datasets: [{
                        data: [C, interets],
                        backgroundColor: ['#3b82f6', '#10b981'],
                        borderWidth: 0
                    }]
                },
                options: {
                    plugins: { legend: { position: 'bottom', labels: { color: 'white' } } }
                }
            });
        }

        // --- 2. Calcul Escompte ---
        function calculerEscompte() {
            const V = parseFloat(document.getElementById('esc_V').value);
            const n = parseFloat(document.getElementById('esc_n').value);
            const t = parseFloat(document.getElementById('esc_t').value);

            if (!V || !n || !t) return alert("Veuillez remplir tous les champs !");

            // e = V * t * n / 36000
            const escompte = (V * t * n) / 36000;
            const net = V - escompte;

            document.getElementById('res_escompte_details').style.display = "block";
            document.getElementById('val_esc_comm').innerText = "- " + escompte.toFixed(2) + " DT";
            document.getElementById('val_esc_net').innerText = net.toFixed(2) + " DT";
        }

        // --- 3. Calcul Emprunt (Tableau) ---
        function calculerEmprunt() {
            const K = parseFloat(document.getElementById('emp_K').value);
            const i_pc = parseFloat(document.getElementById('emp_i').value);
            const n = parseInt(document.getElementById('emp_n').value);

            if (!K || !i_pc || !n) return;

            const i = i_pc / 100;
            // Annuité constante a = K * i / (1 - (1+i)^-n)
            const a = K * (i / (1 - Math.pow(1 + i, -n)));

            let html = "";
            let capitalRestant = K;

            for(let an = 1; an <= n; an++) {
                let interet = capitalRestant * i;
                let amortissement = a - interet;
                
                // Dernier ajustement
                if (an === n) { amortissement = capitalRestant; }
                
                let finRestant = capitalRestant - amortissement;

                html += `
                    <tr>
                        <td>${an}</td>
                        <td>${capitalRestant.toFixed(2)}</td>
                        <td style="color:#f87171">${interet.toFixed(2)}</td>
                        <td style="color:#10b981">${amortissement.toFixed(2)}</td>
                        <td style="font-weight:bold; color:#3b82f6">${(amortissement+interet).toFixed(2)}</td>
                    </tr>
                `;
                capitalRestant = finRestant;
            }
            document.querySelector('#table_emprunt tbody').innerHTML = html;
        }

        // --- 4. Calcul Portefeuille ---
        function updatePfDisplay(val) {
            document.getElementById('lbl_w1').innerText = val;
            document.getElementById('lbl_w2').innerText = 100 - val;
        }

        function calculerPortefeuille() {
            const r1 = parseFloat(document.getElementById('pf_r1').value);
            const r2 = parseFloat(document.getElementById('pf_r2').value);
            const w1_pc = parseFloat(document.getElementById('pf_w1').value);

            const w1 = w1_pc / 100;
            const w2 = 1 - w1;

            const globalReturn = (w1 * r1) + (w2 * r2);

            document.getElementById('res_pf').style.display = "block";
            document.getElementById('pf_global').innerText = globalReturn.toFixed(2) + " %";

            // Graphique
            const ctx = document.getElementById('chartPf').getContext('2d');
            if (chartPfInstance) chartPfInstance.destroy();

            chartPfInstance = new Chart(ctx, {
                type: 'bar',
                data: {
                    labels: ['Actif A', 'Actif B', 'Portfolio'],
                    datasets: [{
                        label: 'Rendement %',
                        data: [r1, r2, globalReturn],
                        backgroundColor: ['#3b82f6', '#8b5cf6', '#10b981'],
                        borderRadius: 5
                    }]
                },
                options: {
                    scales: {
                        y: { beginAtZero: true, ticks: { color: 'white' }, grid: { color: 'rgba(255,255,255,0.1)' } },
                        x: { ticks: { color: 'white' }, grid: { display: false } }
                    },
                    plugins: { legend: { display: false } }
                }
            });
        }
    </script>
</body>
</html>